import { MarkMatchedWordsPipe } from './mark-matched-words.pipe';

describe('MarkMatchedWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkMatchedWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
