import { AddLangToSlugPipe } from './add-lang-to-slug.pipe';

describe('AddLangToSlugPipe', () => {
  it('create an instance', () => {
    const pipe = new AddLangToSlugPipe();
    expect(pipe).toBeTruthy();
  });
});
