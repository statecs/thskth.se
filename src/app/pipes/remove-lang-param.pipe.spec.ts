import { RemoveLangParamPipe } from './remove-lang-param.pipe';

describe('RemoveLangParamPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveLangParamPipe();
    expect(pipe).toBeTruthy();
  });
});
