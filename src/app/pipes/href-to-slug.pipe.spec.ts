import { HrefToSlugPipe } from './href-to-slug.pipe';

describe('HrefToSlugPipe', () => {
  it('create an instance', () => {
    const pipe = new HrefToSlugPipe();
    expect(pipe).toBeTruthy();
  });
});
